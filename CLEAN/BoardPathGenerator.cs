using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BoardPathGenerator : MonoBehaviour
{
    // Start is called before the first frame update
    GameObject Table;

    bool ReachedEnd = false;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
